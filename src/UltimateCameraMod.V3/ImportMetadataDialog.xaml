<Window x:Class="UltimateCameraMod.V3.ImportMetadataDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import — Preset Details" Width="520" Height="680" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource BgDarkBrush}">
    <StackPanel Margin="28">
        <TextBlock Text="Preset details" FontSize="16" FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4"/>
        <TextBlock x:Name="SourceHintLabel" FontSize="11"
                   Foreground="{StaticResource TextDimBrush}" TextWrapping="Wrap"
                   Margin="0,0,0,20"/>

        <TextBlock Text="Name *" FontSize="12" FontWeight="SemiBold"
                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
        <TextBox x:Name="NameBox" Height="44" FontSize="15"
                 Background="{StaticResource BgInputBrush}"
                 Foreground="{StaticResource TextPrimaryBrush}"
                 BorderBrush="{StaticResource BorderBrush}"
                 Padding="12,8" VerticalContentAlignment="Center"
                 Margin="0,0,0,16"/>

        <TextBlock Text="Author (optional)" FontSize="12"
                   Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,6"/>
        <TextBox x:Name="AuthorBox" Height="44" FontSize="15"
                 Background="{StaticResource BgInputBrush}"
                 Foreground="{StaticResource TextPrimaryBrush}"
                 BorderBrush="{StaticResource BorderBrush}"
                 Padding="12,8" VerticalContentAlignment="Center"
                 Margin="0,0,0,16"/>

        <TextBlock Text="Description (optional)" FontSize="12"
                   Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,6"/>
        <TextBox x:Name="DescriptionBox" Height="90" FontSize="14"
                 Background="{StaticResource BgInputBrush}"
                 Foreground="{StaticResource TextPrimaryBrush}"
                 BorderBrush="{StaticResource BorderBrush}"
                 Padding="12,10" TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,16"/>

        <TextBlock Text="URL — Nexus / GitHub / etc. (optional)" FontSize="12"
                   Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,6"/>
        <TextBox x:Name="UrlBox" Height="44" FontSize="15"
                 Background="{StaticResource BgInputBrush}"
                 Foreground="{StaticResource TextPrimaryBrush}"
                 BorderBrush="{StaticResource BorderBrush}"
                 Padding="12,8" VerticalContentAlignment="Center"
                 Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SubtleButton}"
                    Height="36" FontSize="12" Width="90"
                    Click="OnCancel" IsCancel="True"/>
            <Button Content="Import" Style="{StaticResource AccentButton}"
                    Height="36" FontSize="13" Width="110"
                    Click="OnConfirmImport" IsDefault="True" Margin="8,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>
