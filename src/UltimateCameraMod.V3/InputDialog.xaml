<Window x:Class="UltimateCameraMod.V3.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Width="460" Height="240" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource BgDarkBrush}">
    <Grid Margin="28,24,28,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Grid.Row="0" FontSize="15" Foreground="{StaticResource TextPrimaryBrush}"
                   TextWrapping="Wrap" Margin="0,0,0,12"/>

        <TextBox x:Name="InputBox" Grid.Row="1" FontSize="20"
                 Background="{StaticResource BgInputBrush}"
                 Foreground="{StaticResource TextPrimaryBrush}"
                 BorderBrush="{StaticResource BorderBrush}"
                 Padding="12,10" VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Style="{StaticResource SubtleButton}" Width="90" Height="36"
                    Click="OnCancel" IsCancel="True"/>
            <Button Content="OK" Style="{StaticResource AccentButton}" Width="90" Height="36"
                    Click="OnOk" IsDefault="True" Margin="8,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>

